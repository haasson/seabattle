Разметить два поля 10х10
// Создаем поле и через цикл заполняем клетками, подписываем поля
Создать свои корабли
// Создаем функцию, которая знает сколько и каких кораблей надо разместить. Расставляем кликами по полям через слушатель событий
Создать корабли соперника
// Создаем функцию автоматического размещения кораблей. Выбирается рандомная точка и следующие за ней несколько координат по х или у.
Основной цикл игры
    Наш ход
    // Ловим клик по полю соперника и обрабатываем его. 
        Проверка попадание/промах
        // Если поле имеет класс "корабль" - попадание, иначе промах
            если промах - ход завершен
            // конец функции "наш ход"
            если попадание - продолжение хода
                проверка уничтожен ли корабль полностью
                // если ячейки с классом корабль3-2 отсутствует на поле - корабля нет
                    если да - окружить точками
                    // задать класс "мимо" всем ячейкам с коорд. х и у от -1 до 1, если не имеют класс "корабль"
    Ход соперника
        Генерация выстрела по свободной клетке
        // Выбираем случайное поле на карте. Если оно имеет класс "убитый корабль" или "мимо" - повторяем поиск
        Проверка попадание/промах
            если промах - переход хода
            // Завершение функции "ход соперника"
            если попадание - продолжение хода
                проверка уничтожен ли корабль
                    если да - обнести точками, запустить рандомный выстрела
                    если нет - выстрел по соседней клетке
                    // Запускает функцию выстрела по соседней клетке. Если есть два соседних попадания - стреляет по координате, которая изменяется.
Подсчет набранных очков
    Если игрок достиг победного значения - объявляется победитель
    // За каждый класс "корабль" игрок изначально получает одно очко, и при каждой потере части корабля теряет их. Когда очки = 0 - игрок проиграл. Проверять нужно после каждого выстрела.
